function qube (number){
    
    console.log('value of parameter:',number);
    const qube = number * number * number;
    console.log('value of qube:',qube);
}
qube(2);
qube(6);
qube(10);
qube(8);

// double
function double (age){
    const  agemodifier = age*2;
    
    console.log(agemodifier);
}

const jahidAge =25;
double(jahidAge);

